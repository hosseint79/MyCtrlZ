@model IEnumerable<DataLayer.RolePermission>

@{
    ViewData["Title"] = "ShowRolePermission";
    Layout = "~/Views/Shared/_PanelLayout.cshtml";
}

<div class="col-md-8 col-12 p-0">
    <div>
        <a class="btn btn-success" href="/Authorize/CreateRolePermission/@ViewBag.roleid"> create </a>
    </div>
    <section class="table-holder">
        <table class="tabel-content">

            <tr>
                <th>
                    عملیات
                </th>
                <th>
                    سطح دسترسی
                </th>
                <th>
                    نقش
                </th>

            </tr>


            @foreach (var item in Model)
            {
            <tr>
                <td>
                    <a href="/Authorize/DeleteRolePermission/@item.RolePermissionId " class="btn btn-danger" onclick="DeleteClick(@item.RolePermissionId)" data-toggle="modal" data-target="#myModal"> delete </a>
                </td>
                <td>
                    @item.Permission.PermissionName
                </td>
                <td>
                    @item.Role.RoleName
                </td>

            </tr>
            }

        </table>

    </section>


</div>
<div class="modal fade" id="myModal">
    <div class="modal-dialog">
        <div class="modal-content">

            <!-- Modal Header -->
            <div class="p-2 border-bottom">
                <button type="button" class="btn btn-danger" data-dismiss="modal">&times;</button>
            </div>

            <!-- Modal body -->
            <div class="modal-body py-4">
                <div id="ModalBody">

                </div>
            </div>

            <!-- Modal footer -->

        </div>
    </div>
</div>
@section ModalScript{
    <script>
        function DeleteClick(id) {
            $.get("/Authorize/DeleteRolePermission/" + id, function (res) {
                ModalBody.innerHTML = res

            })
        }
    </script>
}
